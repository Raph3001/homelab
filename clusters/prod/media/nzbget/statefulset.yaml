apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: nzbget
spec:
  selector:
    matchLabels:
      app: nzbget
  serviceName: nzbget
  replicas: 1
  template:
    metadata:
      labels:
        app: nzbget
    spec:
      containers:
        - name: nzbget
          image: lscr.io/linuxserver/nzbget:latest
          ports:
            - name: http
              containerPort: 6789
          volumeMounts:
            - name: config
              mountPath: /config
            - name: downloads
              mountPath: /downloads
            - name: incomplete
              mountPath: /incomp
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: config
        - name: downloads
          persistentVolumeClaim:
            claimName: downloads
        - name: incomplete
          persistentVolumeClaim:
            claimName: incomplete
