apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: koito-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "10Gi"
  storageClassName: longhorn
  volumeMode: Filesystem
