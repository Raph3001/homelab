apiVersion: v1
kind: Service
metadata:
  name: koito-alex
spec:
  selector:
    app: koito-alex
  ports:
    - name: http
      port: 80
      targetPort: http

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: koito-alex-tailscale
  annotations:
    tailscale.com/funnel: "true" 
spec:
  defaultBackend:
    service:
      name: koito-alex
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
      - koito-alex
