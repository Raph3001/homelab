apiVersion: v1
kind: PersistentVolume
metadata:
  name: youtube
spec:
  capacity:
    storage: "5Ti"
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /mnt/hdd/shared/mediacenter/media/youtube
    type: Directory

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: youtube
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "5Ti"
  storageClassName: ""
  volumeName: youtube
