apiVersion: v1
kind: Service
metadata:
  name: minecraft-jan
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "minecraft-jan"   # how it appears in your tailnet
    tailscale.com/funnel: "true"
spec:
  selector:
    app: minecraft-jan
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
    - name: minecraft
      port: 443
      targetPort: 25565
      protocol: TCP
