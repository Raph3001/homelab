apiVersion: v1
kind: Service
metadata:
  name: minecraft-jan
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "minecraft-jan"
    tailscale.com/funnel: "true"
spec:
  selector:
    app: minecraft-jan
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
    - name: minecraft
      port: 25565
      targetPort: 25565
      protocol: TCP
